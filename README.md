فرق بین var final -const
var برای همه نوع متغیر است و می توان بار ها مقدار دهی شود
const مثل var می باشد فرقی که دارد فقط می توان یک بار مقدار دهی شود
final مثل const است 
const => compail بررسی می شود
final => runtime بررسی می شود
/////////////////////////////
if 
switchخطی

